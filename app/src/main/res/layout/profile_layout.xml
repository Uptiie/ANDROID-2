<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="#ffffff"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tv_username_profile"
        android:text="@string/username"
        style="@style/tv_login_register"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <EditText
        android:id="@+id/et_username_profile"
        style="@style/et_box"
        android:maxLines="1"
        android:inputType="text"
        app:layout_constraintTop_toBottomOf="@id/tv_username_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <TextView
        android:id="@+id/tv_phone_number_profile"
        android:text="@string/phone_number"
        style="@style/tv_login_register"
        app:layout_constraintTop_toBottomOf="@id/et_username_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <EditText
        android:id="@+id/et_phone_number_profile"
        style="@style/et_box"
        android:maxLines="1"
        android:inputType="phone"
        app:layout_constraintTop_toBottomOf="@id/tv_phone_number_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <TextView
        android:id="@+id/tv_register_password_profile"
        android:text="@string/password"
        style="@style/tv_login_register"
        app:layout_constraintTop_toBottomOf="@id/et_phone_number_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <EditText
        android:id="@+id/et_register_password_profile"
        style="@style/et_box"
        android:maxLines="1"
        android:inputType="textPassword"
        app:layout_constraintTop_toBottomOf="@id/tv_register_password_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <TextView
        android:id="@+id/tv_register_confirm_password_profile"
        android:text="@string/confirm_password"
        style="@style/tv_login_register"
        app:layout_constraintTop_toBottomOf="@id/et_register_password_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <EditText
        android:id="@+id/et_register_confirm_password_profile"
        style="@style/et_box"
        android:maxLines="1"
        android:inputType="textPassword"
        app:layout_constraintTop_toBottomOf="@id/tv_register_confirm_password_profile"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <Button
        android:id="@+id/btn_update"
        android:text="@string/update"
        style="@style/button"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/et_register_confirm_password_profile"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>